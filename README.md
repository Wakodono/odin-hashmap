# Hash map (in progress)

A hash map in JavaScript for [The Odin Project](https://www.theodinproject.com/lessons/javascript-hashmap).

Work so far: factory with load factor `0.75`, capacity `16`, and a `hash(key)` that returns a bucket index.

```bash
npm install
npm test
```
